import {
	zIndex,
	palette,
	typography,
	spacing,
	classes,
	borders,
	device,
	size
} from './themeConfig';

export const theme = {
	palette,
	typography,
	spacing,
	zIndex,
	classes,
	borders,
	device,
	size
};

export type Theme = typeof theme;
