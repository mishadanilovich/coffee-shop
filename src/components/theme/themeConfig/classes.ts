import { spacing } from './spacing';

export const classes = {
	container: {
		padding: spacing(5, 35)
	},
	contentCenter: {
		display: 'flex',
		alignItems: 'center',
		justifyContent: 'center'
	}
};
